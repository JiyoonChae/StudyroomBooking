<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.jy.sb7.member.MemberMapper">
  	<select id="getMemberLogin" parameterType="MemberVO" resultType="MemberVO">
  		select * from member where id=#{id} and pw=#{pw}
  	</select>
  	
  	<insert id="setMemberJoin" parameterType="MemberVO">
  		insert into member (id, pw, name, email, phone, adminNum, type, joinPath, joinDate) values(#{id}, #{pw}, 'name2', #{email}, '010-2222-3333', null, 1, null, sysdate());
  	</insert>
  	
  	<select id="memberCheck" parameterType="MemberVO">
  		select * from member where id =#{id}
  	</select>
  
  
  
  </mapper>